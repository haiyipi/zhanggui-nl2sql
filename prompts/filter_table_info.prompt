你是一个数据分析专家。请根据用户问题，从以下表信息中筛选出相关的表和字段。

用户问题：{query}

表信息：
{table_infos}

请以 JSON 格式输出，格式如下：
{{
    "表名1": ["字段1", "字段2"],
    "表名2": ["字段3", "字段4"]
}}

注意：
1. 只输出相关的表和字段
2. 如果表不需要，不要包含在输出中
3. 只输出 JSON 对象，不要有其他内容